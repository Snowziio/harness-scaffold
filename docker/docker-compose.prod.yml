services:
  app:
    image: ${IMAGE}
    ports:
      - "${APP_PORT:-8000}:8000"
    environment:
      - ENV=production
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"
