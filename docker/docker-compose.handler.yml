services:
  checkpoint-handler:
    image: ${ACR_REGISTRY}/${ACR_NAMESPACE}/checkpoint-handler:${IMAGE_TAG:-latest}
    ports:
      - "8002:8002"
    environment:
      - FEISHU_VERIFICATION_TOKEN=${FEISHU_VERIFICATION_TOKEN}
      - GITHUB_PAT=${GITHUB_PAT}
    restart: unless-stopped
